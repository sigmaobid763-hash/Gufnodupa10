services:
  ubuntu:
    image: lscr.io/linuxserver/webtop:ubuntu-xfce
    container_name: ubuntu
    environment:
      - PUID=1000
      - PGID=1000
      - TZ=Europe/Warsaw
    ports:
      - 3000:3000
    restart: unless-stopped